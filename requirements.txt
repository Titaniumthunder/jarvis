# Jarvis main-venv dependencies (Python 3.14).
#
# Install with:
#   python3.14 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# MuJoCo lives in a separate Python 3.10 venv — see requirements-mujoco.txt.

# ─── Web server ───────────────────────────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
pydantic>=2.0,<3.0

# ─── LLM clients ──────────────────────────────────────────────────────────────
# Groq is the primary fast LLM; google-genai is the legacy/fallback path.
# requests is also used to talk to a local Ollama server (no SDK).
groq>=1.0,<2.0
google-genai>=1.0,<2.0
requests>=2.30,<3.0

# ─── Voice ────────────────────────────────────────────────────────────────────
# Whisper runs locally — no API. PyAudio drives the mic.
openai-whisper>=20240930
PyAudio>=0.2,<1.0

# ─── Image generation ─────────────────────────────────────────────────────────
diffusers>=0.30,<1.0
torch>=2.5,<3.0
transformers>=4.40,<6.0
accelerate>=0.30,<2.0
Pillow>=11.0
rembg>=2.0,<3.0

# ─── 3D / TripoSR ─────────────────────────────────────────────────────────────
# These cover what triposr_src/tsr/{system,utils,models}.py imports on the
# Jarvis code path. (gradio, moderngl, xatlas — used only by TripoSR's demo
# scripts — are intentionally omitted.)
trimesh>=4.0,<5.0
omegaconf>=2.3,<3.0
einops>=0.7,<1.0
imageio>=2.30,<3.0
huggingface_hub>=0.24,<2.0
torchmcubes>=0.1,<1.0
numpy>=1.24

# ─── Web search ───────────────────────────────────────────────────────────────
# Brave Search API is hit directly via urllib (stdlib) — no extra package.

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0
